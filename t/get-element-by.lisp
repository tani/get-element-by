(in-package :cl-user)
(defpackage get-element-by-test
  (:use :cl
        :get-element-by
        :prove))
(in-package :get-element-by-test)

;; NOTE: To run this test file, execute `(asdf:test-system :get-element-by)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
